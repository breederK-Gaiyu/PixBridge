# PixBridge v1.6.1 开发计划

## 📋 问题总结

### v1.6 当前问题
- ✅ 图片导入：正常工作
- ❌ 视频导入：无法导入到达芬奇媒体池
- ❌ 音频导入：无法导入到达芬奇媒体池

### 根本原因分析
复制文件到本地后，通过 `media_pool.ImportMedia()` 导入时，达芬奇 API 对视频/音频文件返回 `None` 或空列表，可能是：
1. 文件格式/编码不兼容
2. 达芬奇项目设置限制
3. 文件复制过程中的问题

## 🎯 v1.6.1 解决方案

### 核心思路
**分离图片和视频/音频的处理策略**：

1. **图片**：继续使用剪贴板读取 + 本地保存 + 导入（保持 v1.5 兼容）
2. **视频/音频**：改用原始路径直接导入（不复制到本地）

### 优势
- ✅ 避免大文件复制，节省磁盘空间
- ✅ 导入速度更快
- ✅ 直接使用原文件，保持最佳质量
- ✅ 绕过文件复制可能导致的兼容性问题

## 📐 技术方案

### 1. 修改 `batch_import()` 方法

**当前逻辑**（v1.6）：
```python
def batch_import(self, files):
    # 所有文件类型都：
    # 1. 复制到本地分类文件夹
    # 2. 导入复制后的文件到达芬奇
```

**新逻辑**（v1.6.1）：
```python
def batch_import(self, files):
    # 按类型分组
    images = [f for f in files if f['type'] == 'image']
    videos = [f for f in files if f['type'] == 'video']
    audios = [f for f in files if f['type'] == 'audio']

    # 图片：复制到本地 + 导入副本
    if images:
        self.import_images_with_copy(images)

    # 视频/音频：直接导入原路径
    if videos:
        self.import_media_direct(videos, 'video')
    if audios:
        self.import_media_direct(audios, 'audio')
```

### 2. 创建新方法 `import_media_direct()`

```python
def import_media_direct(self, files, media_type):
    """
    直接从原路径导入视频/音频到达芬奇

    优点：
    1. 不占用额外磁盘空间
    2. 导入速度更快
    3. 保持原始文件质量
    4. 避免复制导致的兼容性问题

    Args:
        files: 文件信息列表
        media_type: 'video' 或 'audio'
    """

    self.log(f"📂 处理 {len(files)} 个 {media_type} 文件...")

    # 去重检查
    filtered_files = []
    for file_info in files:
        file_hash = self.calculate_file_hash(file_info['path'])

        if file_hash is None:
            continue

        if file_hash in self.processed_hashes:
            self.log(f"ℹ️ 已导入过，跳过: {file_info['name']}")
            continue

        self.processed_hashes.add(file_hash)
        filtered_files.append(file_info)

    if not filtered_files:
        return

    # 提取原始路径（字符串列表）
    original_paths = [str(f['path']) for f in filtered_files]

    # 记录日志
    for f in filtered_files:
        self.log(f"🎬 检测到{media_type}: {f['name']}")
        self.log(f"📍 原始路径: {f['path']}")

    # 直接导入到达芬奇
    success_count = self.import_to_resolve_batch(
        original_paths,
        media_type
    )

    if success_count > 0:
        self.import_count[media_type] += success_count
        self.log(f"✅ 成功导入 {success_count} 个 {media_type} 文件")
        self.log(f"💡 提示: 文件未复制到本地，请勿移动或删除原文件")

        # 更新UI
        QTimer.singleShot(0, self.update_stats_ui)
```

### 3. 拆分 `import_images_with_copy()` 方法

将当前 `batch_import()` 中的图片处理逻辑提取为独立方法：

```python
def import_images_with_copy(self, images):
    """
    复制图片到本地并导入到达芬奇（v1.5 兼容）

    Args:
        images: 图片文件信息列表
    """

    self.log(f"📂 处理 {len(images)} 个图片文件...")

    # 去重
    filtered_images = []
    for file_info in images:
        file_hash = self.calculate_file_hash(file_info['path'])
        if file_hash and file_hash not in self.processed_hashes:
            self.processed_hashes.add(file_hash)
            filtered_images.append(file_info)

    if not filtered_images:
        return

    # 创建本地文件夹
    type_folder = os.path.join(self.save_dir, MEDIA_FOLDERS['image'])
    if not os.path.exists(type_folder):
        os.makedirs(type_folder)

    # 复制文件到本地
    copied_files = []
    for file_info in filtered_images:
        try:
            timestamp = int(time.time() * 1000)
            new_filename = f"clip_{timestamp}_{file_info['name']}"
            dest_path = os.path.join(type_folder, new_filename)

            shutil.copy2(file_info['path'], dest_path)
            file_size = os.path.getsize(dest_path) / (1024 * 1024)
            self.log(f"💾 已保存: <code>{new_filename}</code> ({file_size:.2f} MB)")

            copied_files.append(dest_path)
            time.sleep(0.001)

        except Exception as e:
            self.log(f"❌ 复制失败: {e}")

    # 导入到达芬奇
    if copied_files:
        file_paths_str = [str(f) for f in copied_files]
        success_count = self.import_to_resolve_batch(file_paths_str, 'image')

        if success_count > 0:
            self.import_count['image'] += success_count
            self.log(f"✅ 成功导入 {success_count} 个图片到达芬奇")
            QTimer.singleShot(0, self.update_stats_ui)
```

## 🔄 导入流程对比

### v1.6 当前流程（所有类型）
```
复制文件 → 检测剪贴板 → 计算哈希去重
→ 复制到本地分类文件夹 → 导入副本到达芬奇
```

### v1.6.1 新流程

**图片**：
```
复制图片 → 检测剪贴板 → 计算哈希去重
→ 复制到 Clipboard_Images/ → 导入到达芬奇 Clipboard_Images
```

**视频/音频**：
```
复制视频/音频 → 检测剪贴板 → 获取原始路径 → 计算哈希去重
→ 直接导入原文件到达芬奇 Clipboard_Videos/Clipboard_Audios
→ 不保存副本到本地
```

## 📊 日志输出优化

### 图片导入日志（保持原样）
```
[时间] 📦 检测到 1 个已选择类型的文件
[时间]   - photo.jpg (image, 0.50 MB)
[时间] 🚀 开始批量导入 1 个文件...
[时间] 📂 处理 1 个 image 文件...
[时间] 💾 已保存: clip_xxx_photo.jpg (0.50 MB)
[时间] ✅ 成功导入 1 个图片到达芬奇
```

### 视频/音频导入日志（新增）
```
[时间] 📦 检测到 1 个已选择类型的文件
[时间]   - video.mp4 (video, 15.30 MB)
[时间] 🚀 开始批量导入 1 个文件...
[时间] 📂 处理 1 个 video 文件...
[时间] 🎬 检测到video: video.mp4
[时间] 📍 原始路径: /Users/xxx/Movies/video.mp4
[时间] 📁 已创建达芬奇媒体文件夹: Clipboard_Videos
[时间] ✅ 成功导入 1 个 video 文件
[时间] 💡 提示: 文件未复制到本地，请勿移动或删除原文件
```

## ⚠️ 注意事项

### 1. 文件移动风险
- 用户移动或删除原文件后，达芬奇项目会丢失素材
- 需要在日志中明确提示用户

### 2. 外部存储警告
- 如果文件在外部硬盘，拔出后会丢失
- 未来可以添加"警告：文件位于外部存储"提示

### 3. 向后兼容
- 完全兼容 v1.5 的图片功能
- 图片依然会复制到本地
- 不影响现有工作流程

## 🗂️ 文件结构变化

### 本地文件夹（v1.6.1）
```
~/Downloads/Clipboard_Assets/
├── Clipboard_Images/          # 图片副本（继续保存）
│   ├── clip_xxx_photo1.jpg
│   └── clip_xxx_photo2.png
├── Clipboard_Videos/          # 空（不再保存副本）
└── Clipboard_Audios/          # 空（不再保存副本）
```

### 达芬奇媒体池（v1.6.1）
```
媒体池 (Media Pool)
├── Clipboard_Images/          # 图片素材（来自本地副本）
│   ├── clip_xxx_photo1.jpg
│   └── clip_xxx_photo2.png
├── Clipboard_Videos/          # 视频素材（链接到原文件）
│   ├── video1.mp4 -> /Users/xxx/Movies/video1.mp4
│   └── video2.mov -> /Users/xxx/Videos/video2.mov
├── Clipboard_Audios/          # 音频素材（链接到原文件）
│   └── song.mp3 -> /Users/xxx/Music/song.mp3
└── Clipboard_Assets/          # v1.5 兼容文件夹
    └── clip_xxx.png
```

## 📅 实施计划

### 明天的任务清单

1. **代码修改**（预计 1 小时）
   - [ ] 重构 `batch_import()` 方法
   - [ ] 创建 `import_media_direct()` 方法
   - [ ] 提取 `import_images_with_copy()` 方法
   - [ ] 优化日志输出

2. **测试**（预计 30 分钟）
   - [ ] 测试图片导入（确保 v1.5 兼容）
   - [ ] 测试视频直接导入
   - [ ] 测试音频直接导入
   - [ ] 测试混合批量导入
   - [ ] 测试去重功能

3. **文档更新**（预计 30 分钟）
   - [ ] 创建 `V1.6.1_CHANGELOG.md`
   - [ ] 更新 `README.md`
   - [ ] 创建 `DIRECT_IMPORT_GUIDE.md`

4. **Git 提交**
   - [ ] 提交 v1.6.1 代码
   - [ ] 推送到 GitHub
   - [ ] 创建 v1.6.1 标签

## 🎯 成功标准

- ✅ 图片导入功能完全正常（v1.5 兼容）
- ✅ 视频可以成功导入到达芬奇媒体池
- ✅ 音频可以成功导入到达芬奇媒体池
- ✅ 去重功能正常工作
- ✅ 分类文件夹自动创建
- ✅ 日志清晰易懂
- ✅ 向后兼容 v1.5

## 💡 未来优化（可选）

### Phase 2（可选功能）
- [ ] 添加配置选项：是否保存视频/音频副本
- [ ] 添加外部存储检测和警告
- [ ] 添加"复制到本地"按钮（手动触发）

### Phase 3（增强功能）
- [ ] 显示文件来源路径
- [ ] 支持批量重新链接丢失的素材
- [ ] 自动检测文件移动并更新路径

---

**准备就绪，明天开始实施 v1.6.1！** 🚀

**预计完成时间**: 2-3 小时
**优先级**: 高
**风险**: 低（向后兼容，不影响现有功能）
