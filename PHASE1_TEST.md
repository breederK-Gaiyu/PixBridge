# PixBridge v1.6 Phase 1 测试指南

## ✅ Phase 1 已完成功能

1. ✅ 支持检测图片、视频、音频文件
2. ✅ 文件类型自动识别
3. ✅ 批量文件检测（显示文件信息）
4. ✅ 文件哈希计算（为 Phase 3 去重做准备）

## 🧪 测试步骤

### 准备工作
```bash
cd /Users/wukaiyu/Documents/03\ Coding/CursorCoding/claude/DR_PixBridge
python3 PixBridge_v1.6_phase1.py
```

### 测试 1: 单个图片文件
1. 在 Finder 中找一个 `.jpg` 或 `.png` 图片文件
2. 右键选择"复制"（或选中后按 `Cmd+C`）
3. 切换到 PixBridge 窗口

**预期结果：**
```
[时间] 📦 检测到 1 个文件
[时间]   - example.jpg (image, 0.50 MB)
[时间] ℹ️ 批量导入功能将在 Phase 3 实现
```

### 测试 2: 单个视频文件
1. 在 Finder 中找一个 `.mp4` 或 `.mov` 视频文件
2. 复制文件（`Cmd+C`）

**预期结果：**
```
[时间] 📦 检测到 1 个文件
[时间]   - video_sample.mp4 (video, 15.30 MB)
[时间] ℹ️ 批量导入功能将在 Phase 3 实现
```

### 测试 3: 单个音频文件
1. 在 Finder 中找一个 `.mp3` 或 `.wav` 音频文件
2. 复制文件（`Cmd+C`）

**预期结果：**
```
[时间] 📦 检测到 1 个文件
[时间]   - song.mp3 (audio, 3.45 MB)
[时间] ℹ️ 批量导入功能将在 Phase 3 实现
```

### 测试 4: 批量多个文件
1. 在 Finder 中同时选中：
   - 1 个图片文件（.jpg）
   - 1 个视频文件（.mp4）
   - 1 个音频文件（.mp3）
2. 一起复制（`Cmd+C`）

**预期结果：**
```
[时间] 📦 检测到 3 个文件
[时间]   - photo.jpg (image, 0.50 MB)
[时间]   - clip.mp4 (video, 15.30 MB)
[时间]   - track.mp3 (audio, 3.45 MB)
[时间] ℹ️ 批量导入功能将在 Phase 3 实现
```

### 测试 5: 不支持的文件类型
1. 复制一个 `.txt` 或 `.pdf` 文件

**预期结果：**
```
[时间] ℹ️ 不支持的文件类型: document.pdf
```

### 测试 6: 向后兼容 - 从浏览器复制图片
1. 打开浏览器（Chrome/Safari）
2. 右键点击网页上的图片，选择"拷贝图像"
3. 切换到 PixBridge

**预期结果：**
```
[时间] 🖼️ 检测到新的复制操作，正在检查图片...
[时间] ✅ 检测到新图片 (哈希: 3f2a1b4c...)
[时间] 💾 图片已保存: clip_1711234567890.png (125.3 KB)
[时间] 🎞️ 成功导入到达芬奇: clip_1711234567890.png
```

## ✅ 通过标准

- [ ] 能正确识别图片文件（.jpg, .png, .gif 等）
- [ ] 能正确识别视频文件（.mp4, .mov, .avi 等）
- [ ] 能正确识别音频文件（.mp3, .wav, .aac 等）
- [ ] 能同时检测多个文件
- [ ] 能正确显示文件名、类型和大小
- [ ] 不支持的文件类型会被跳过
- [ ] v1.5 的浏览器图片复制功能依然正常工作

## 📋 已知限制（Phase 1）

1. **只检测不导入**：Phase 1 只实现文件检测，显示文件信息，不会实际导入到达芬奇
2. **没有类型选择器**：暂时检测所有类型，Phase 2 将添加 UI 选择器
3. **没有去重**：文件哈希计算已实现，但去重逻辑将在 Phase 3 添加

## 🚀 下一步

Phase 1 测试通过后，将进入：
- **Phase 2**: 添加媒体类型选择器 UI（图片/视频/音频复选框）
- **Phase 3**: 实现完整的批量导入功能

---

**测试完成后请反馈结果！**
