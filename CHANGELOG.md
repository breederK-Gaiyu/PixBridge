# PixBridge 更新日志

## v1.5 (2026-03-22) - 重大更新

### 🎉 核心改进

#### 1. 智能去重机制（核心功能）
- **问题**：之前版本会不停地导入剪贴板中已存在的图片
- **解决方案**：
  - 实现基于 MD5 的图片内容哈希去重
  - 只在检测到**新的复制操作**（Cmd+C）时触发导入
  - 相同内容的图片只会导入一次
  - 启动时剪贴板中已有的内容不会被导入

#### 2. 缓存管理系统
- 新增"清除缓存"功能按钮
- 实时显示缓存记录数量
- 支持手动重置去重记录
- 会话级别缓存（关闭程序后自动清空）

### ✨ 新增功能

1. **改进的监听机制**
   - 初始化时记录当前剪贴板状态
   - 只响应新的复制事件
   - 添加友好的提示信息

2. **增强的状态面板**
   - 显示已导入图片数量
   - 显示缓存记录数量
   - 实时更新状态信息

3. **更好的用户体验**
   - 双按钮布局（清除缓存 + 停止监听）
   - 缓存清除后弹窗提示
   - 详细的日志消息

### 🐛 Bug 修复

1. **修复 QApplication 初始化错误**
   - 从 `QApplication(sys.path)` 改为 `QApplication(sys.argv)`

2. **修复重复导入问题**
   - 添加图片哈希验证
   - 防止同一图片多次导入

### 📖 文档改进

1. 完整的中文代码注释
2. 详细的功能说明文档
3. 新增故障排查章节
4. 使用示例和界面预览

### 🔧 技术细节

#### 新增依赖
```python
import hashlib
from io import BytesIO
```

#### 核心方法
1. `calculate_image_hash(img)` - 计算图片 MD5 哈希
2. `clear_cache()` - 清除哈希缓存
3. 优化的 `monitor_loop()` - 改进的监控循环
4. 增强的 `do_import()` - 带去重检查的导入逻辑

#### 数据结构
```python
# 图片哈希去重集合
self.processed_image_hashes = set()

# 最后处理的剪贴板计数
self.last_processed_change_count = None
```

### 🎨 界面改进

```
之前：
[开启实时监听]

现在：
[🗑️ 清除缓存]  [⏹️ 停止监听]
```

状态面板新增：
- 缓存记录: X 个

---

## v1.4 (之前版本)

### 基础功能
- ✅ 剪贴板实时监控
- ✅ 自动保存到本地
- ✅ 自动导入到达芬奇
- ✅ 媒体文件夹管理
- ✅ PyQt6 深色界面
- ✅ 实时日志系统

### 已知问题（已在 v1.5 修复）
- ❌ 启动后会重复导入剪贴板已有内容
- ❌ 相同图片会被重复导入
- ❌ 缺少去重机制

---

## 升级建议

### 从 v1.4 升级到 v1.5

1. **备份旧版本**（可选）
   ```bash
   cp "DVR_IMGPaster1.4.py" "DVR_IMGPaster1.4.py.backup"
   ```

2. **替换新版本**
   ```bash
   cp PixBridge_v1.5.py "/Library/Application Support/Blackmagic Design/DaVinci Resolve/Fusion/Scripts/Edit/"
   ```

3. **验证功能**
   - 启动脚本，观察日志
   - 复制一张图片，确认导入成功
   - 再次复制同一张图片，应该跳过
   - 点击"清除缓存"，再次复制应该成功导入

### 主要差异对比

| 功能 | v1.4 | v1.5 |
|------|------|------|
| 去重机制 | ❌ | ✅ 基于哈希 |
| 启动重复导入 | ❌ 会重复 | ✅ 已修复 |
| 缓存管理 | ❌ | ✅ 手动清除 |
| 状态显示 | 基础 | ✅ 详细统计 |
| 代码注释 | 部分 | ✅ 完整中文 |
| 文档 | 简单 | ✅ 详细说明 |

---

## 未来计划 (Roadmap)

### v1.6 规划
- [ ] 支持批量导入多张图片
- [ ] 图片预览功能
- [ ] 自定义文件命名规则
- [ ] 导入历史记录
- [ ] 支持更多图片格式（WEBP、HEIC等）
- [ ] 快捷键支持
- [ ] Windows 系统支持

### v2.0 愿景
- [ ] 视频剪贴板支持
- [ ] 云同步功能
- [ ] 团队协作功能
- [ ] 插件系统

---

**问题反馈**：如遇到问题，请查看 README.md 中的故障排查章节
