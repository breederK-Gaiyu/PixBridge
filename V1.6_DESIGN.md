# PixBridge v1.6 设计文档

## 🎯 版本目标

从 v1.5 的单一图片导入升级为**多媒体自动导入系统**

---

## 📋 核心功能

### 1. 多媒体类型支持
```python
SUPPORTED_TYPES = {
    'image': ['.png', '.jpg', '.jpeg', '.gif', '.bmp', '.tiff', '.webp'],
    'video': ['.mp4', '.mov', '.avi', '.mkv', '.flv', '.wmv', '.m4v'],
    'audio': ['.mp3', '.wav', '.aac', '.flac', '.m4a', '.ogg', '.wma']
}
```

### 2. UI 布局结构
```
┌─────────────────────────────────────────────────────┐
│ 🖼️ PixBridge v1.6 - 多媒体剪贴板自动导入工具       │
├─────────────────────────────────────────────────────┤
│ 📂 媒体类型选择                                     │
│   [✓] 🖼️ 图片   [✓] 🎬 视频   [✓] 🎵 音频        │
│   [全选] [取消全选]                                 │
├─────────────────────────────────────────────────────┤
│ [主控制面板] [导入历史]                            │
│                                                     │
│ 📁 本地保存路径                                     │
│ 📂 [~/Downloads/Clipboard_Assets] [浏览] [打开]    │
│                                                     │
│ 📊 状态信息                                        │
│ 🟢 监听中...                                        │
│ 🖼️ 图片: 5   🎬 视频: 2   🎵 音频: 1             │
│ 缓存记录: 8 个                                      │
│                                                     │
│ 📝 运行日志                                        │
│ [日志内容区域]                                      │
│                                                     │
│   [🗑️ 清除缓存]  [⏹️ 停止监听]                    │
└─────────────────────────────────────────────────────┘
```

### 3. 数据结构

#### 导入历史记录
```python
history_entry = {
    'timestamp': '2026-03-23 10:30:45',
    'filename': 'video_sample.mp4',
    'media_type': 'video',
    'file_size': '15.3 MB',
    'file_path': '/path/to/file',
    'status': '成功',  # 成功/失败
    'error': None
}
```

#### 文件分类存储
```
~/Downloads/Clipboard_Assets/
├── Clipboard_Images/     # 图片文件夹
│   ├── clip_1711234567890.png
│   └── clip_1711234567891.jpg
├── Clipboard_Videos/     # 视频文件夹
│   ├── clip_1711234567892.mp4
│   └── clip_1711234567893.mov
└── Clipboard_Audios/     # 音频文件夹
    ├── clip_1711234567894.mp3
    └── clip_1711234567895.wav
```

---

## 🔧 技术实现

### 1. 文件类型检测

#### macOS 剪贴板文件检测
```python
def detect_clipboard_files(self):
    """检测剪贴板中的文件"""
    if not self.pb:
        return []

    # 获取文件 URL 列表
    file_urls = self.pb.propertyListForType_(AppKit.NSFilenamesPboardType)

    if not file_urls:
        return []

    files = []
    for url in file_urls:
        file_path = Path(url)
        media_type = self.detect_media_type(file_path)
        if media_type:
            files.append({
                'path': file_path,
                'type': media_type
            })

    return files
```

#### 媒体类型判断
```python
def detect_media_type(self, file_path):
    """根据扩展名判断媒体类型"""
    ext = file_path.suffix.lower()

    if ext in SUPPORTED_FORMATS['image']:
        return 'image'
    elif ext in SUPPORTED_FORMATS['video']:
        return 'video'
    elif ext in SUPPORTED_FORMATS['audio']:
        return 'audio'

    return None
```

### 2. 批量导入流程

```
用户复制多个文件
    ↓
检测剪贴板变化
    ↓
获取文件列表 (NSFilenamesPboardType)
    ↓
遍历每个文件
    ├→ 检测文件类型
    ├→ 检查是否启用该类型
    ├→ 计算文件哈希
    ├→ 检查是否重复
    └→ 复制到对应文件夹
    ↓
并行导入到达芬奇
    ├→ Clipboard_Images
    ├→ Clipboard_Videos
    └→ Clipboard_Audios
    ↓
更新UI和历史记录
```

### 3. 达芬奇多文件夹管理

```python
def import_to_resolve_categorized(self, file_list):
    """分类导入到达芬奇"""
    media_pool = self.get_media_pool()

    # 按类型分组
    grouped = {
        'image': [],
        'video': [],
        'audio': []
    }

    for file_info in file_list:
        grouped[file_info['type']].append(file_info['path'])

    # 分别导入到不同文件夹
    for media_type, files in grouped.items():
        if not files:
            continue

        folder_name = MEDIA_FOLDERS[media_type]
        target_folder = self.get_or_create_folder(media_pool, folder_name)
        media_pool.SetCurrentFolder(target_folder)
        media_pool.ImportMedia([str(f) for f in files])
```

---

## 📊 功能对比

| 功能 | v1.5 | v1.6 |
|------|------|------|
| 图片导入 | ✅ | ✅ |
| 视频导入 | ❌ | ✅ |
| 音频导入 | ❌ | ✅ |
| 类型选择 | ❌ | ✅ |
| 批量导入 | ❌ | ✅ |
| 自动分类 | ❌ | ✅ |
| 导入历史 | ❌ | ✅ |
| 历史导出 | ❌ | ✅ |
| Tab 界面 | ❌ | ✅ |
| 分类统计 | ❌ | ✅ |

---

## 🚀 实现优先级

### Phase 1: 核心功能 ✅
1. ✅ 多媒体类型支持（检测机制）
2. ✅ 类型选择器 UI
3. ✅ 批量文件导入
4. ✅ 自动分类存储

### Phase 2: 高级功能
5. ✅ 导入历史记录
6. ✅ Tab 界面布局
7. ✅ 分类统计显示
8. ⏳ 历史导出功能

### Phase 3: 增强功能（v1.7）
9. ⏳ 文件预览面板
10. ⏳ 格式过滤器
11. ⏳ 缩略图显示
12. ⏳ 拖拽导入

---

## 📝 关键代码片段

### 监控循环改进

```python
def monitor_loop(self):
    """监控剪贴板文件变化"""
    last_count = self.pb.changeCount()

    while self.is_running:
        current_count = self.pb.changeCount()

        if current_count != last_count:
            # 检测文件
            files = self.detect_clipboard_files()

            if files:
                # 过滤已选择的类型
                selected_types = self.media_selector.get_selected_types()
                filtered_files = [
                    f for f in files
                    if f['type'] in selected_types
                ]

                if filtered_files:
                    self.log(f"📦 检测到 {len(filtered_files)} 个文件")
                    QTimer.singleShot(0, lambda: self.batch_import(filtered_files))

            last_count = current_count

        time.sleep(0.5)
```

---

## 🎨 UI 设计原则

1. **一致性**: 保持 v1.5 的深色主题风格
2. **清晰性**: 类型选择器放在顶部，一目了然
3. **效率性**: Tab 切换，不占用太多空间
4. **反馈性**: 实时显示各类型统计

---

## 🔄 升级路径

### 从 v1.5 升级
- ✅ 兼容原有配置
- ✅ 自动迁移历史记录
- ✅ 保留原有功能
- ✅ 新增功能可选

### 配置文件
```json
{
  "version": "1.6",
  "enabled_types": ["image", "video", "audio"],
  "save_directory": "~/Downloads/Clipboard_Assets",
  "history": []
}
```

---

**设计完成，开始实现完整代码！**
