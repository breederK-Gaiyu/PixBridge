# PixBridge v1.6 Phase 2 测试指南

## ✅ Phase 2 已完成功能

1. ✅ 支持检测图片、视频、音频文件（Phase 1）
2. ✅ 文件类型自动识别（Phase 1）
3. ✅ 批量文件检测（Phase 1）
4. ✅ 媒体类型选择器 UI（Phase 2）
5. ✅ 类型过滤功能（Phase 2）

## 🧪 测试步骤

### 准备工作
```bash
cd /Users/wukaiyu/Documents/03\ Coding/CursorCoding/claude/DR_PixBridge
python3 PixBridge_v1.6_phase2.py
```

### 测试 1: UI 显示检查
启动程序后，检查界面：

**预期结果：**
- 顶部显示 "PixBridge v1.6-phase2 - 多媒体剪贴板自动导入工具"
- 标题栏下方出现 "📂 媒体类型选择" 区域
- 包含三个复选框：
  - 🖼️ 图片（默认勾选）
  - 🎬 视频（默认未勾选）
  - 🎵 音频（默认未勾选）
- 有 "全选" 和 "取消全选" 按钮

### 测试 2: 默认设置 - 只检测图片
1. 保持默认设置（只勾选 🖼️ 图片）
2. 点击 "▶️ 开启实时监听"
3. 在 Finder 中复制一个 `.jpg` 图片文件

**预期结果：**
```
[时间] 📦 检测到 1 个已选择类型的文件
[时间]   - photo.jpg (image, 0.50 MB)
[时间] ℹ️ 批量导入功能将在 Phase 3 实现
```

4. 在 Finder 中复制一个 `.mp4` 视频文件

**预期结果：**
```
[时间] ℹ️ 检测到 1 个文件，但都不是已选择的类型
```

### 测试 3: 全选功能
1. 点击 "全选" 按钮
2. 确认三个复选框都被勾选了
3. 复制一个 `.mp3` 音频文件

**预期结果：**
```
[时间] 📦 检测到 1 个已选择类型的文件
[时间]   - song.mp3 (audio, 3.45 MB)
[时间] ℹ️ 批量导入功能将在 Phase 3 实现
```

### 测试 4: 只选择视频
1. 点击 "取消全选" 按钮
2. 只勾选 🎬 视频
3. 复制一个 `.jpg` 图片

**预期结果：**
```
[时间] ℹ️ 检测到 1 个文件，但都不是已选择的类型
```

4. 复制一个 `.mp4` 视频

**预期结果：**
```
[时间] 📦 检测到 1 个已选择类型的文件
[时间]   - video.mp4 (video, 15.30 MB)
[时间] ℹ️ 批量导入功能将在 Phase 3 实现
```

### 测试 5: 多类型选择
1. 勾选 🖼️ 图片 和 🎬 视频（不勾选音频）
2. 同时复制：
   - 1 个 `.jpg` 图片
   - 1 个 `.mp4` 视频
   - 1 个 `.mp3` 音频

**预期结果：**
```
[时间] 📦 检测到 2 个已选择类型的文件
[时间]   - photo.jpg (image, 0.50 MB)
[时间]   - video.mp4 (video, 15.30 MB)
[时间] ℹ️ 批量导入功能将在 Phase 3 实现
```
（注意：音频文件被过滤掉，不显示）

### 测试 6: 取消全选
1. 点击 "取消全选" 按钮
2. 确认三个复选框都未勾选
3. 复制任何文件

**预期结果：**
```
[时间] ℹ️ 检测到 1 个文件，但都不是已选择的类型
```

### 测试 7: 向后兼容 - 浏览器图片复制
1. 勾选 🖼️ 图片
2. 打开浏览器，右键点击网页图片，选择"拷贝图像"

**预期结果：**
```
[时间] 🖼️ 检测到新的复制操作，正在检查图片...
[时间] ✅ 检测到新图片 (哈希: 3f2a1b4c...)
[时间] 💾 图片已保存: clip_1711234567890.png (125.3 KB)
[时间] 🎞️ 成功导入到达芬奇: clip_1711234567890.png
```

如果取消勾选 🖼️ 图片，浏览器图片复制将不会触发导入。

### 测试 8: 动态切换类型
1. 开启监听
2. 勾选 🖼️ 图片，复制图片文件 → 应该显示
3. 取消勾选 🖼️ 图片，再复制同一个图片文件 → 应该被过滤
4. 重新勾选 🖼️ 图片，复制视频文件 → 应该被过滤
5. 勾选 🎬 视频，复制视频文件 → 应该显示

## ✅ 通过标准

- [ ] 类型选择器 UI 正确显示
- [ ] 默认只勾选图片
- [ ] "全选" 按钮功能正常
- [ ] "取消全选" 按钮功能正常
- [ ] 单独勾选每种类型时，过滤功能正常
- [ ] 多种类型同时勾选时，正确显示所有选中类型
- [ ] 未勾选的类型会被过滤掉
- [ ] 浏览器图片复制功能依然正常
- [ ] 可以动态切换类型，无需重启程序

## 📋 Phase 2 新增功能

### 1. MediaTypeSelector 组件
位置：主界面顶部（标题栏下方）

**功能：**
- 三个复选框控制图片/视频/音频的检测
- 全选/取消全选快捷按钮
- 实时生效，无需重启

### 2. 类型过滤逻辑
在 `monitor_loop()` 中实现：

```python
# 获取选中的类型
selected_types = self.media_selector.get_selected_types()

# 过滤文件
filtered_files = [f for f in files if f['type'] in selected_types]
```

### 3. 智能提示
- 检测到文件但类型未选中时，提示：
  ```
  ℹ️ 检测到 X 个文件，但都不是已选择的类型
  ```

## 🚀 下一步

Phase 2 测试通过后，将进入：
- **Phase 3**: 实现完整的批量导入功能
  - 批量复制文件到本地
  - 自动分类到不同文件夹
  - 批量导入到达芬奇的对应媒体文件夹
  - 文件去重

---

**测试完成后请反馈结果！**
