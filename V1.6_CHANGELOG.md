# PixBridge v1.6 更新日志

## 🚀 版本信息
- **版本号**: v1.6
- **发布日期**: 2026-03-24
- **类型**: 功能增强版本

## 📝 更新摘要

v1.6 新增了**图片批量处理功能**，现在可以一次性选择多张图片并批量导入到达芬奇！

## ✨ 新增功能

### 图片批量导入 ⭐⭐⭐⭐⭐

**功能描述**：
支持同时复制/选择多张图片，一次性批量导入到达芬奇。

**使用场景**：
```
用户在 Finder 中选中 10 张图片
→ 按 ⌘+C 复制
→ PixBridge 自动检测到 10 张图片
→ 批量保存到本地
→ 批量导入到达芬奇
```

**日志示例**：
```
[14:30:00] 📦 检测到 5 张图片
[14:30:01] 💾 [1/5] 已保存: clip_1711348425123.png (1.2 MB)
[14:30:01] 💾 [2/5] 已保存: clip_1711348426234.png (0.8 MB)
[14:30:01] 💾 [3/5] 已保存: clip_1711348427345.png (1.5 MB)
[14:30:02] ℹ️ [4/5] 已导入过，跳过: photo4.jpg
[14:30:02] 💾 [5/5] 已保存: clip_1711348428456.png (0.9 MB)
[14:30:02] 📤 开始导入 4 张图片到达芬奇...
[14:30:03] 🎞️ 批量导入成功: 4 张图片
[14:30:03] ✅ 成功导入 4 张图片到达芬奇
[14:30:03] 📊 总结: 导入 4 张，跳过 1 张（已存在）
```

## 🔧 技术改进

### 1. 新增 `detect_clipboard_images()` 方法

**功能**：检测剪贴板中的图片（支持单张和多张）

**实现**：
```python
def detect_clipboard_images(self):
    """
    检测剪贴板中的图片（支持单张和多张）

    Returns:
        list: 图片列表
    """
    images = []

    # 方法1: 检测文件路径（支持多文件）
    if AppKit:
        pasteboard = AppKit.NSPasteboard.generalPasteboard()
        file_urls = pasteboard.propertyListForType_(AppKit.NSFilenamesPboardType)

        if file_urls:
            for url in file_urls:
                file_path = Path(url)
                if file_path.suffix.lower() in SUPPORTED_IMAGE_FORMATS:
                    images.append(('file', file_path))

            if images:
                return images

    # 方法2: 检测剪贴板图片数据（单张）
    img = ImageGrab.grabclipboard()
    if img:
        images.append(('clipboard', img))

    return images
```

### 2. 重构 `do_import()` 方法

**改进**：
- 支持检测和处理多张图片
- 显示进度信息（[1/5]、[2/5] 等）
- 统计跳过的图片数量
- 显示批量导入总结

**新增日志**：
- `📦 检测到 X 张图片`
- `💾 [X/Y] 已保存: ...`
- `ℹ️ [X/Y] 已导入过，跳过: ...`
- `📤 开始导入 X 张图片到达芬奇...`
- `📊 总结: 导入 X 张，跳过 Y 张（已存在）`

### 3. 新增 `batch_import_to_resolve()` 方法

**功能**：批量导入图片到达芬奇媒体池

**实现**：
```python
def batch_import_to_resolve(self, files):
    """
    批量导入图片到达芬奇媒体池（v1.6 新增）

    Args:
        files: 文件列表，每个元素是 (filepath, filename) 元组

    Returns:
        int: 成功导入的文件数量
    """
    # ... 连接达芬奇 ...

    # 批量导入媒体文件
    file_paths = [filepath for filepath, filename in files]
    imported_clips = media_pool.ImportMedia(file_paths)

    if imported_clips:
        success_count = len(imported_clips) if isinstance(imported_clips, list) else 1
        return success_count

    return 0
```

**优势**：
- 一次性调用达芬奇 API，效率更高
- 减少 API 调用次数
- 统一处理成功和失败

### 4. 支持的图片格式

**新增常量**：
```python
SUPPORTED_IMAGE_FORMATS = {
    '.png', '.jpg', '.jpeg', '.gif', '.bmp',
    '.tiff', '.webp', '.heic'
}
```

**支持的格式**：
- PNG, JPG, JPEG, GIF, BMP
- TIFF, WebP, HEIC

## 📊 功能对比

| 功能 | v1.5 | v1.6 | 改进 |
|------|------|------|------|
| 单张图片导入 | ✅ | ✅ | 保持一致 |
| 多张图片导入 | ❌ | ✅ **新增** | 批量处理 |
| 进度显示 | ❌ | ✅ **新增** | [X/Y] 格式 |
| 批量导入API | ❌ | ✅ **新增** | 效率提升 |
| 导入总结 | ❌ | ✅ **新增** | 统计信息 |
| 去重功能 | ✅ | ✅ | 保持一致 |
| 浏览器图片 | ✅ | ✅ | 保持一致 |

## 🎯 使用方法

### 单张图片导入（v1.5 兼容）

1. 在任意应用中复制一张图片
2. PixBridge 自动检测并导入
3. 与 v1.5 完全一致

### 批量图片导入（v1.6 新增）

1. 在 Finder 中选中多张图片
2. 按 ⌘+C 复制
3. PixBridge 自动检测并批量导入
4. 查看导入进度和总结

## ⚠️ 注意事项

### 兼容性

- ✅ 完全向后兼容 v1.5
- ✅ 单张图片导入功能不受影响
- ✅ 哈希去重机制保持不变
- ✅ 达芬奇 API 调用兼容

### 性能

- ✅ 批量导入比单张导入更快
- ✅ 减少达芬奇 API 调用次数
- ✅ 统一处理，减少开销

### 限制

- 批量导入仅支持从 Finder 复制文件
- 浏览器直接复制依然是单张图片
- macOS 专属功能（使用 AppKit）

## 🔄 升级指南

### 从 v1.5 升级到 v1.6

1. **下载新版本**：
   ```bash
   # 下载 PixBridge_v1.6.py
   ```

2. **运行新版本**：
   ```bash
   python3 PixBridge_v1.6.py
   ```

3. **测试批量功能**：
   - 选择 3-5 张图片
   - 按 ⌘+C 复制
   - 查看批量导入效果

### 配置迁移

- ✅ 保存路径配置自动迁移
- ✅ 已导入图片哈希记录保持有效
- ✅ 无需任何配置修改

## 🐛 已知问题

目前无已知问题。

## 📋 下一步计划

Phase 1 还有两个功能待实现：

1. **快捷键支持** ⭐⭐⭐⭐⭐
   - ⌘+Shift+V 立即导入
   - ⌘+Shift+P 暂停/继续

2. **格式自动转换** ⭐⭐⭐⭐
   - 自动转换 WebP/HEIC → PNG

## 📚 相关文档

- [ENHANCEMENT_PLAN.md](ENHANCEMENT_PLAN.md) - 完整功能增强计划
- [README.md](README.md) - 项目总览

---

**v1.6 - 让批量导入更简单！** 🚀
