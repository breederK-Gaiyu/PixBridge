# PixBridge 快速开始指南

## 🚀 5 分钟快速上手

### 第一步：确认环境

```bash
# 检查 Python 版本（需要 3.8+）
python3 --version

# 检查系统（需要 macOS）
uname -s  # 应该显示 Darwin
```

### 第二步：安装依赖

```bash
# 安装必要的 Python 库
pip3 install pillow PyQt6
```

### 第三步：测试运行

```bash
# 进入项目目录
cd /Users/wukaiyu/Documents/03\ Coding/CursorCoding/claude/DR_PixBridge

# 运行脚本测试
python3 PixBridge_v1.5.py
```

**预期结果**：
- ✅ 弹出 PixBridge 窗口
- ✅ 界面显示深色主题
- ✅ 日志显示"PixBridge v1.5 已启动"

### 第四步：连接达芬奇

1. **启动 DaVinci Resolve**
2. **打开或创建一个项目**
3. **启用外部脚本**：
   - 偏好设置 → 系统 → 通用
   - 勾选"外部脚本使用"

4. **在 PixBridge 中观察日志**：
   - ✅ "已连接到达芬奇项目: [项目名]"

### 第五步：测试导入

1. **点击"开启实时监听"按钮**
2. **从浏览器复制一张图片**（右键 → 复制图片，或 Cmd+C）
3. **观察 PixBridge 日志**：

```
[23:30:12] 🖼️ 检测到新的复制操作，正在检查图片...
[23:30:12] ✅ 检测到新图片 (哈希: a3f2c1d8...)
[23:30:12] 💾 图片已保存: clip_1711122612345.png (245.3 KB)
[23:30:13] 🎞️ 成功导入到达芬奇: clip_1711122612345.png
```

4. **在达芬奇媒体池中查看**：
   - 找到 "Clipboard_Assets" 文件夹
   - 图片应该已经出现在里面

---

## 🎯 常用操作

### 操作 1：正常使用流程

```
1. 启动达芬奇并打开项目
2. 运行 PixBridge
3. 点击"开启实时监听"
4. 复制图片（Cmd+C）
5. 图片自动导入达芬奇
```

### 操作 2：重新导入相同图片

```
1. 点击"清除缓存"按钮
2. 再次复制该图片
3. 图片会重新导入
```

### 操作 3：更改保存路径

```
1. 点击"浏览"按钮
2. 选择新的文件夹
3. 新的图片会保存到新路径
```

### 操作 4：查看已保存的图片

```
1. 点击"打开文件夹"按钮
2. Finder 会打开保存位置
```

---

## ⚠️ 常见问题速查

### 问题：点击"开启监听"后无反应

**检查清单**：
- [ ] 是否在 macOS 系统上运行？
- [ ] Python 版本是否 >= 3.8？
- [ ] 是否安装了 pillow 和 PyQt6？

```bash
# 重新安装依赖
pip3 install --upgrade pillow PyQt6
```

### 问题：图片保存成功但未导入达芬奇

**检查清单**：
- [ ] 达芬奇是否正在运行？
- [ ] 是否打开了项目？
- [ ] 是否启用了"外部脚本使用"？

**解决方案**：
```
1. 在达芬奇中：编辑 → 偏好设置 → 系统 → 通用
2. 勾选"外部脚本使用"
3. 重启达芬奇
4. 重新运行 PixBridge
```

### 问题：相同图片一直被跳过

**原因**：去重机制正在工作

**解决方案**：
```
点击"清除缓存"按钮即可重新导入
```

### 问题：界面显示不正常/不是深色主题

**解决方案**：
```bash
# 确保使用正确的 Python 版本
python3 PixBridge_v1.5.py

# 如果还是有问题，尝试更新 PyQt6
pip3 install --upgrade PyQt6
```

---

## 📦 安装到达芬奇（可选）

如果希望从达芬奇菜单直接启动 PixBridge：

```bash
# 复制到达芬奇脚本目录
cp PixBridge_v1.5.py "/Library/Application Support/Blackmagic Design/DaVinci Resolve/Fusion/Scripts/Edit/"
```

然后在达芬奇中：
- **工作区** → **脚本** → **PixBridge_v1.5**

---

## 💡 使用技巧

### 技巧 1：从网页快速导入参考图

```
1. 浏览设计网站（如 Behance、Dribbble）
2. 看到喜欢的图片，右键 → 复制图片
3. 图片自动出现在达芬奇媒体池
4. 直接拖到时间线使用
```

### 技巧 2：截图即导入

```
1. 使用 macOS 截图工具（Cmd+Shift+4）
2. 按 Control 键，截图会复制到剪贴板
3. 截图自动导入达芬奇
```

### 技巧 3：批量工作流

```
1. 开启 PixBridge 监听
2. 在浏览器中依次复制多张图片
3. 所有图片自动导入
4. 在达芬奇中整理使用
```

### 技巧 4：避免重复导入

```
PixBridge 会自动记住已导入的图片
相同内容的图片只会导入一次
如需重新导入，点击"清除缓存"
```

---

## 🔍 验证安装成功

### 完整测试清单

```
✅ 步骤 1：脚本能正常启动
   - 运行 python3 PixBridge_v1.5.py
   - 窗口正常显示

✅ 步骤 2：连接达芬奇成功
   - 日志显示"已连接到达芬奇项目"

✅ 步骤 3：监听功能正常
   - 点击"开启实时监听"
   - 按钮变为"停止监听"
   - 日志显示"服务已启动"

✅ 步骤 4：复制检测正常
   - 复制一张图片
   - 日志显示"检测到新的复制操作"

✅ 步骤 5：保存功能正常
   - 日志显示"图片已保存"
   - 本地文件夹出现图片文件

✅ 步骤 6：达芬奇导入正常
   - 日志显示"成功导入到达芬奇"
   - 媒体池中出现 Clipboard_Assets 文件夹
   - 文件夹中有刚导入的图片

✅ 步骤 7：去重功能正常
   - 再次复制同一张图片
   - 日志显示"该图片已经导入过，跳过重复导入"

✅ 步骤 8：清除缓存正常
   - 点击"清除缓存"
   - 弹出提示窗口
   - 缓存记录变为 0
```

**全部通过 = 安装成功！** 🎉

---

## 📞 获取帮助

- 📖 查看完整文档：[README.md](README.md)
- 📝 查看更新日志：[CHANGELOG.md](CHANGELOG.md)
- ⚠️ 遇到问题：查看 README 中的"故障排查"章节

**祝您使用愉快！** 🚀
